import { PushDataDirective } from './push-data.directive';

describe('PushDataDirective', () => {
  it('should create an instance', () => {
    const directive = new PushDataDirective();
    expect(directive).toBeTruthy();
  });
});
